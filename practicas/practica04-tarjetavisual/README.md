# 🌟 Práctica 04: Tarjeta de Perfil Profesional

## 🎯 Objetivo de la Práctica
El objetivo principal de esta práctica fue la aplicación avanzada de **CSS3** para el diseño de una interfaz modular e interactiva con enfoque profesional. Se buscó:
1.  **Separación de Archivos:** Mantener la separación de responsabilidades utilizando `index.html` y `styles.css`.
2.  **Animación 3D (Flip Card):** Implementar el efecto de giro de tarjeta mediante transformaciones (`rotateY`, `transform-style`) y perspectiva.
3.  **Diseño Profesional:** Crear un diseño limpio y estructurado (tipo *dashboard*) utilizando colores institucionales (blanco, gris y azul) para una presentación formal.
4.  **Maquetación Semántica:** Utilizar etiquetas como `<section>`, `<header>`, y `<figure>` para una mejor organización del contenido.

## 💻 Tecnologías Utilizadas
* **HTML5:** Para la maquetación semántica de la tarjeta (perfil, datos, habilidades).
* **CSS3 (styles.css):** Para la animación de giro 3D, el diseño tipo perfil, y la aplicación de estilos de contraste.

## ⚙️ Instrucciones para Ejecutar
1.  Asegúrate de que los archivos `index.html` y `styles.css` se encuentren en esta carpeta.
2.  Abre el archivo **`index.html`** directamente en el navegador web.
3.  Pasa el cursor (mouse) sobre la tarjeta para activar la animación de giro y ver la información de la cara trasera.